<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.apiiungo.mapper.FavoriteMapper">

    <insert id="insertFavorite" parameterType="com.apiiungo.entity.Favorite" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO favorite (user_id, target_type, target_id, create_time)
        VALUES (#{userId}, #{targetType}, #{targetId}, #{createTime})
    </insert>

    <delete id="deleteFavorite">
        DELETE FROM favorite
        WHERE user_id = #{userId}
          AND target_type = #{targetType}
          AND target_id = #{targetId}
    </delete>

    <select id="countByTarget" resultType="int">
        SELECT COUNT(*)
        FROM favorite
        WHERE target_type = #{targetType}
          AND target_id = #{targetId}
    </select>

    <select id="exists" resultType="int">
        SELECT COUNT(*)
        FROM favorite
        WHERE user_id = #{userId}
          AND target_type = #{targetType}
          AND target_id = #{targetId}
    </select>

</mapper>
