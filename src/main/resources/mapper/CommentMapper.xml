<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.apiiungo.mapper.CommentMapper">

    <insert id="insertComment" parameterType="com.apiiungo.entity.Comment" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO comment (post_id, user_id, content, create_time)
        VALUES (#{postId}, #{userId}, #{content}, #{createTime})
    </insert>

    <select id="selectByPost" resultType="com.apiiungo.entity.Comment">
        SELECT id, post_id AS postId, user_id AS userId, content, create_time AS createTime
        FROM comment WHERE post_id = #{postId} ORDER BY create_time ASC
    </select>



</mapper>
