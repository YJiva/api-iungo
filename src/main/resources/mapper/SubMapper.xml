<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.apiiungo.mapper.SubMapper">

    <resultMap id="SubResult" type="com.apiiungo.entity.Sub">
        <id property="id" column="id"/>
        <result property="userId" column="user_id"/>
        <result property="message" column="message"/>
        <result property="read" column="read"/>
        <result property="createTime" column="create_time"/>
    </resultMap>

    <insert id="insert" parameterType="com.apiiungo.entity.Sub" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO sub (user_id, message, `read`, create_time)
        VALUES (#{userId}, #{message}, #{read, jdbcType=TINYINT}, #{createTime})
    </insert>

    <select id="selectByUser" parameterType="long" resultMap="SubResult">
        SELECT *
        FROM sub
        WHERE user_id = #{userId}
        ORDER BY `read` ASC, create_time DESC
    </select>

    <update id="markRead">
        UPDATE sub
        SET `read` = 1
        WHERE id = #{id}
          AND user_id = #{userId}
    </update>

    <update id="markAllRead">
        UPDATE sub
        SET `read` = 1
        WHERE user_id = #{userId}
          AND `read` = 0
    </update>

</mapper>

